main(List<String> args) {
  //Set
  //set is an unordered collection so there is no index present for set
  //Set only takes unique values even if u have any duplicate value it will not print it.
  Set names = {'Sameer', 'Vaibhaw', 'Avinash'};
  print(names);
  Set abc = Set();
  print(abc.runtimeType);

  //Set can be best used for storing unique elements
}
