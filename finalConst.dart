main(List<String> args) {
  final name = 'dart';
  //We don't need to assign name and use as String, it is done by Type Inference.
  const use = 'flutter';

  //final and const act same outside a class that is in a function

  print('Name: $name \n Use: $use');
}
